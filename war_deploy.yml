---
- hosts: "{{ group }}"
  become: yes
  gather_facts: no 
#  vars:
#    group: test
#    wars:
#      - { 'name': 's', 'get_url': 'http://172.16.100.191/docs/s.war', 'version': 'v1.0' }

  tasks:
    - import_role:
        name: tomcat
        tasks_from: war
